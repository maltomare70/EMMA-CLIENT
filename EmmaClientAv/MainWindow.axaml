<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="850" d:DesignHeight="600"
        x:Class="EmmaClientAv.MainWindow"
        Title="Emma Client"
        Background="#F5F7FA">

    <Grid RowDefinitions="Auto, *">
        
        <!-- Titolo della Dashboard -->
        <StackPanel Grid.Row="0" Margin="0,35,0,15" HorizontalAlignment="Center">
            <TextBlock Text="Benvenuto" FontSize="28" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
            <TextBlock Text="Seleziona un'operazione per iniziare" FontSize="14" Foreground="#64748B" Margin="0,5,0,0" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Griglia principale dei Pulsanti (2 righe, 3 colonne) -->
        <UniformGrid Grid.Row="1" 
                     Rows="2" 
                     Columns="4" 
                     HorizontalAlignment="Center" 
                     VerticalAlignment="Center"
                     Margin="20"
                     MaxWidth="1000">
            
            <!-- ================= RIGA 1: OPERAZIONI DI CARICAMENTO ================= -->
            
            <!-- CARICA FATTURA -->
            <Button Name="BtnFattura" Width="200" Height="200" Margin="12" Click="Button_OnClick"
                    Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <TextBlock Text="📄" FontSize="42" HorizontalAlignment="Center"/>
                    <TextBlock Text="Carica Documento" FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Name="BtnVisualizza" Width="200" Height="200" Margin="12" Click="Button_OnClick"
                    Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <TextBlock Text="👀" FontSize="42" HorizontalAlignment="Center"/>
                    <TextBlock Text="Visualizza Documenti" FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
            
            
            <Button Name="BtnFornitori" Width="200" Height="200" Margin="12" Click="Button_OnClick"
                    Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <TextBlock Text="🧑‍💼" FontSize="42" HorizontalAlignment="Center"/>
                    <TextBlock Text="Fornitori" FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Name="BtnArticoliFornitori" Width="200" Height="200" Margin="12" Click="Button_OnClick"
                    Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <TextBlock Text="🏷️️" FontSize="42" HorizontalAlignment="Center"/>
                    <TextBlock Text="Articoli" FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
            
            <Button Name="BtnConfigurazione" Width="200" Height="200" Margin="12" Click="Button_OnClick"   IsVisible="False"
                    Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <TextBlock Text="⚙️" FontSize="42" HorizontalAlignment="Center"/>
                    <TextBlock Text="Impostazioni" FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>
            <!-- ================= RIGA 2: GESTIONE E STRUMENTI ================= -->

            <!-- CARD 4: CONCILIAZIONE -->
            <Button Name="BtnConciliazione" Width="180" Height="180" Margin="12" Click="Button_OnClick" IsVisible="False"
                    Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <TextBlock Text="🔄" FontSize="42" HorizontalAlignment="Center"/>
                    <TextBlock Text="Conciliazione" FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- CARD 5: SINCRONIZZAZIONE -->
            <Button Name="BtnSincronizzazione" Width="180" Height="180" Margin="12" Click="Button_OnClick"  IsVisible="False"
                    Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
                    <TextBlock Text="🌐" FontSize="42" HorizontalAlignment="Center"/>
                    <TextBlock Text="Sincronizza" FontSize="16" FontWeight="SemiBold" Foreground="#334155" HorizontalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- CARD 6: CONFIGURAZIONE -->


        </UniformGrid>
    </Grid>
</Window>